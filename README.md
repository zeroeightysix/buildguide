# Buildguide

## Setup

1. Import into IntelliJ using `build.gradle`
2. Run:
    ```
    ./gradlew idea
    ```

## Build

```
./gradlew build
```

## Commands
`/buildguide`

e.g: `/buildguide add sphere 5`